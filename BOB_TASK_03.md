# Bob task 03 — finish evidence correctness and reproducibility

Read BOB_REVIEW_02.md first. Task 02 now runs on Windows: Codex observed 42 passing tests and all six demo outcomes as expected. Original task 02 is preserved in commit 51b4837. Two extra real controls expose false PASS outcomes; close them before presentation. No new product features in this task.

Use native file reading/editing tools only within this project. No terminal/shell commands, parent directories, credentials, installations, publication or extra purchases. Codex runs validation independently. Do not claim tests passed merely because you wrote them. Preserve all frozen validation-fixtures and their evidence.

## Work to finish

1. **Prerequisites affect the result.** Pass the actual fixtureDir and expected packageName from pack to installation. Validate consumer realpath is outside that source, has no ancestor node_modules candidates and has a distinct installed package of the expected name. Inspect that exact expected path, not the first scanned package; use realpath/lstat and path.relative-based containment, including Windows case handling. Failed required isolation/identity/hash prerequisites must stop contract execution when already known and yield INCONCLUSIVE, even when a process exit is zero. Keep process exit and overall outcome as separate facts in the report. Record explicit verification results and reasons. Do not call it a security sandbox.

2. **Link integrity evidence to the bytes executed.** Record hash of the copied contract before and after execution; null, unreadable or changed must produce INCONCLUSIVE while preserving actual process output and exit. Validate relevant installed file bytes against the produced archive; use an explicit verification status, never assume a folder equals its tarball hash. Handle only the trusted local npm archives needed by the demo; reject unsupported archive constructs rather than silently treating them as verified. Resolve and record the package's public entry within the consumer, but remember `import.meta.resolve` can return a missing target. A broken export must still be allowed to run and produce its genuine FAIL; missing advertised entry is the defect being tested, not automatically a runner integrity error. The archive contents themselves must match installed bytes.

3. **Make timeout and skipped-test claims accurate.** Avoid depending on catchable SIGTERM for termination of the direct child (e.g. SIGKILL with spawnSync on supported Windows/POSIX platforms). Retain elapsed-timeout error identification and explicit limits on descendant process handling; do not describe execution as a sandbox. Preserve real error codes, raw output and elapsed duration. In tests, use explicit test-context skips if npm is unavailable or fail a required integration command clearly; never early-return from a test and count it as passing.

4. **Record the actual source baseline once per demo.** Execute the frozen validation-fixtures/source-tests.test.mjs via Node with a bound; save a separate baseline record with command array, script hash, actual exit, output, duration and timestamp. Link it to the demo result without changing individual consumer outcomes. Missing or failed baseline must be visible and prevent claiming the full demonstration is validated. In generic single-case mode, a source check not requested must be explicitly marked not-run, never PASS. Keep demo reproducible offline using only the existing trusted fixtures.

5. **Add targeted regressions and concise README.** Cover the two reproduced false-PASS controls, wrong installed identity/ancestor contamination, missing/changed copied hash and explicit npm-unavailable handling. The original six integration outcomes must remain unchanged. README should explain installation prerequisites, commands for tests/demo/single case, PASS versus expected FAIL versus INCONCLUSIVE, output paths, inspectable temporary data, trusted-fixture-only scope and tested platform limitations. Update PROVENANCE with actual task 03 work and mark old task-state paragraphs as history; no claims of executed task03 tests or captured consumption screenshots.

Use compact helpers and readable code. Do not change frozen fixtures or expectations to obtain green results. If any requested prerequisite is still unsupported, clearly report unverified/inconclusive and identify the limitation rather than declaring the task fully complete.

Finish with changed files, exact validation commands for Codex and honest remaining limitations. Leave viewer, video, PDF and publication for after this verification step.
